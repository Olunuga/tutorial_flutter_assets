import 'package:flutter/material.dart';

class LoadingIconFlutterIcon extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Loading Icons Flutter Icon"),),
      body: Column(
        children: [

        ],
      ),
    );
  }

}